@{
    // Use admin layout for admin pages, front layout for others
    if (Context.Request.Path.Value?.StartsWith("/admin") == true)
    {
        Layout = "_Layout";
    }
    else 
    {
        Layout = "_FrontLayout";
    }
}
