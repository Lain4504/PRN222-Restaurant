@page "/feedbacks"
@model PRN222_Restaurant.Pages.FeedbackModel
@{
    ViewData["Title"] = "Tất cả phản hồi";
    Layout = "_FrontLayout";
}

<div class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto">

        <h2 class="text-2xl font-bold mb-6 text-center text-gray-900">Tất cả phản hồi từ khách hàng</h2>

        @(await Html.RenderComponentAsync<PRN222_Restaurant.Components.FeedbackList>(RenderMode.Server))
    </div>
</div>

